import { StyleSheet } from 'react-native'

const LoginStyle = StyleSheet.create({
  View: {
    padding: 10
  },
  Text: {
    fontSize: 20,
    fontWeight: 'bold',
    color: '#F00'
  }
})

export default LoginStyle
