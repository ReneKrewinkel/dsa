export const DEVICE_TOKEN = 'your-device'

const BACKEND_PLANNING_URL = "https://api.dev-master.ninja/reactjs/slow/"
const DATABASE_NAME = 'dsa_database_v1.db'

export const config = {
  DEVICE_TOKEN,
  BACKEND_PLANNING_URL,
  DATABASE_NAME
}